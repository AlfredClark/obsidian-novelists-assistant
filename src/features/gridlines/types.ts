/** 网格线类名，与排版类叠加生效：样式表仅 .novel-typeset.novel-gridlines 时渲染网格 */
export const GRIDLINES_CLASS = "novel-gridlines";

/** 网格虚线长度 CSS 变量，值由设置页滑块写入，样式表以 :root 默认值兜底消费 */
export const GRIDLINES_SIZE_VAR = "--novel-gridlines-size";

/** 网格虚线间隔 CSS 变量，值由设置页滑块写入，样式表以 :root 默认值兜底消费 */
export const GRIDLINES_SPACE_VAR = "--novel-gridlines-space";

/** 网格线厚度 CSS 变量，值由设置页滑块写入，样式表以 :root 默认值兜底消费 */
export const GRIDLINES_THICK_VAR = "--novel-gridlines-thick";

/** 网格线不透明度 CSS 变量，值由设置页滑块写入，样式表以 :root 默认值兜底消费 */
export const GRIDLINES_OPACITY_VAR = "--novel-gridlines-opacity";
